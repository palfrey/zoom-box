---
environment: vagrant
data_path: .
servers:
    - name: server
      ssh_hostname: localhost
      ssh_port: 2222
      ssh_key: ../../../.vagrant/machines/default/virtualbox/private_key
      ssh_user: vagrant
